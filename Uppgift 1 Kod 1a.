#include <stdio.h>

double subtract(double a, double b)
{
	double c = a - b;

	return c;
}





int main(void)
{
	double a = 3.4; 
	double b = 6.8;
	double c = subtract(a, b);

	printf("%lg - %lg = %lg", a, b, c);

	return 0;
}
