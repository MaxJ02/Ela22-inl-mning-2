#include <stdio.h>


void subtract(double a, double b, double* c)
{
	*c = a - b;
  
	return; 
}




int main(void)
{
	double a = 3.4;
	double b = 6.8;
	double c;
	
	subtract(a, b, &c);

	printf("%lg - %lg = %lg", a, b, c);

	return 0;
  
}
